import styled from "styled-components";

export const StyleRoutes = styled.div`
  font-size: 36px;

  div{
    display: flex;
    justify-content: space-evenly;
    padding: 25px 10px;
    text-decoration: none;
    font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
  }
`